apiVersion: v1
kind: Service
metadata:
  name: {{ include "empfinchart.fullname" . }}
  labels:
    {{- include "empfinchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "empfinchart.selectorLabels" . | nindent 4 }}
